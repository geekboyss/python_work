direct = {
        'print': 'print sentence',
        'for': 'access every element',
        'if': 'condicton'
        }

print("print : " + direct['print'] + '\n' + 
        "for : " + direct['for'] + '\n' +
        "if  : " + direct['if']  
    )
